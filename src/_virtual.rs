//* // 1
// verification-helper: PROBLEM
use proconio::input;

fn main() {
    input! {
        n: usize,
        a: [i32; n],
    }
    let mut ans = 0;
    for &a in a.iter() {
        ans += a;
    }
    println!("{}", ans);
}
// */

/* // 2
// verification-helper: PROBLEM
use proconio::input;

fn main() {
    input! {
        n: usize,
        a: [i32; n],
    }
    let mut ans = 0;
    for &a in a.iter() {
        ans += a;
    }
    println!("{}", ans);
}
// */

/* // 3
// verification-helper: PROBLEM
use proconio::input;

fn main() {
    input! {
        n: usize,
        a: [i32; n],
    }
    let mut ans = 0;
    for &a in a.iter() {
        ans += a;
    }
    println!("{}", ans);
}
// */

/* // 4
// verification-helper: PROBLEM
use proconio::input;

fn main() {
    input! {
        n: usize,
        a: [i32; n],
    }
    let mut ans = 0;
    for &a in a.iter() {
        ans += a;
    }
    println!("{}", ans);
}
// */

/* // 5
// verification-helper: PROBLEM
use proconio::input;

fn main() {
    input! {
        n: usize,
        a: [i32; n],
    }
    let mut ans = 0;
    for &a in a.iter() {
        ans += a;
    }
    println!("{}", ans);
}
// */

/* // 6
// verification-helper: PROBLEM
use proconio::input;

fn main() {
    input! {
        n: usize,
        a: [i32; n],
    }
    let mut ans = 0;
    for &a in a.iter() {
        ans += a;
    }
    println!("{}", ans);
}
// */
